# Components

React components grouped by feature or concern.

| Folder | Purpose |
|---|---|
| `Landing/` | Marketing landing page sections |
| `ui/` | Shared UI primitives (Button, Logo, icons) |

Each subfolder has its own `README.md` describing its files.

Future folders: `Chat/`, `Login/`, `Sidebar/`.
