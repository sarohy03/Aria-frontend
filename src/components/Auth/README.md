# Auth

Authentication context and provider.

| File | Purpose |
|---|---|
| `authContext.js` | React context definition |
| `AuthProvider.jsx` | Subscribes to Firebase auth state, wraps the app |

Use `useAuth()` from `hooks/useAuth.js` in components.
